// Example

// import { SET_AUTH, LOGOUT } from '../types/types';

// export default (state = true, action) => {
//   const { type, payload } = action;

//   switch (type) {
//     default:
//       return state;
//   }
// };
